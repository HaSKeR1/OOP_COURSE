#include <iostream>
#include <algorithm>
#include <vector>

class CLASS_NAME {
public:
    int val = 0;
    CLASS_NAME(int val_) : val(val_) {};
};

int main() {
    std::vector<CLASS_NAME> arr;
    for (int i = 0; i < 7; i++) {

    }
    CLASS_NAME(5);
    [](const CLASS_NAME& lhs, const CLASS_NAME& rhs) { if (lhs.val < rhs.val) { return true; } return false; };
}